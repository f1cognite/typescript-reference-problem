{
  "name": "typescrtipt-reference-problem",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "devDependencies": {
    "rollup": "^0.67.0",
    "rollup-plugin-typescript2": "^0.17.2",
    "typescript": "^3.1.6"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn clean && rollup -c"
  }
}
